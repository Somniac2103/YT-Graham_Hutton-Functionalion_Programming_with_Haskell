Check answers using GHCI

['a','b', 'c']

('a','b', 'c')

[(False,'0'),(True,'1')]

([False,True], ['0','1'])

[tail,inot, reverse]

second xs = head (tail xs)

swap (x,y) = (y,X)

pair x y = (x,y)

double x = x*2

palindrome xs = reverse xs == xs

twice f x = f (f x)